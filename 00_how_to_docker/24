docker service create --name orbital-command --detach -e RABBITMQ_DEFAULT_USER=user -e RABBITMQ_DEFAULT_PASS=password rabbitmq

# create      - Create a new service
# --network   - Network attachments

# service	  - container

# https://hub.docker.com/_/rabbitmq